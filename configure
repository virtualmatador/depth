#!/bin/bash

echo "Depth configuring ..."

packages=\
"
    g++
    libpng-dev
"
libs=\
"
"
cd prerequisite/
if ! ./configure.sh "${packages}" "${libs}"
then
    echo "Depth configuration failed."
    exit -1
fi
cd ../

echo "Depth configured."
